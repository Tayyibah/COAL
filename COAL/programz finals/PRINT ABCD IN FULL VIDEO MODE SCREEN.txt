.MODEL SMALL
.STACK 100H
.DATA
NUMS1 dB  'ABCDEFGHIJKLMNOPQRSTUVWXYZ$'

.CODE
MAIN PROC
	MOV AX,@DATA
	MOV DS,AX

	MOV SI ,158
	MOV CX ,26    
	MOV BX ,0     
	MOV AX,0B800H
	MOV ES,AX
	MOV DI,OFFSET NUMS1
	DEC CX
	DEC BX
FOR1:
MOV BX,0
	WHILE11:
		CMP BX,81
	JE END_WHILE1_
	MOV DH,16H
	MOV DL,[DI]
	MOV ES:[SI],DX
	INC BX
	ADD SI,2
	JMP WHILE11

END_WHILE1_: 
	;ADD SI,2
	INC DI
LOOP FOR1
	MOV AH ,4CH
	INT 21H
	MAIN ENDP
END MAIN




